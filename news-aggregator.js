const STORAGE={savedSignals:'ceiSavedSignals',talking:'ceiTalkingPoints',predictions:'ceiPredictions',briefs:'ceiBriefItems',reviewed:'ceiArticlesReviewed',dashboard:'cei.home.dashboard.v2'};
const weeklyGoal=20;let activeFilter='All';let articles=[];let sourceStatus=[];
const fallbackArticles=[{id:'fallback-boc',title:'Bank of Canada feed unavailable fallback',source:'Bank of Canada',url:'https://www.bankofcanada.ca/rss-feeds/',link:'https://www.bankofcanada.ca/rss-feeds/',category:'Rates',tier:'Tier 1',relevance:4,relevanceScore:78,relevanceLevel:'Medium',highRelevance:false,chilliwackRelevant:true,fraserValleyRelevant:false,impact:'Unclassified',why:'Check source manually. Rate and monetary policy news may affect borrowing conditions and housing demand.',cedarbrookImpact:'Check source manually. Rate and monetary policy news may affect borrowing conditions and housing demand.',predictionHint:'If rate conditions ease or buyer confidence improves, Cedarbrook absorption assumptions may strengthen.',summary:''},{id:'fallback-cmhc',title:'CMHC feed unavailable fallback',source:'CMHC',url:'https://www.cmhc-schl.gc.ca/media-newsroom/cmhc-news-room-rss',link:'https://www.cmhc-schl.gc.ca/media-newsroom/cmhc-news-room-rss',category:'Housing',tier:'Tier 1',relevance:4,relevanceScore:82,relevanceLevel:'High',highRelevance:true,chilliwackRelevant:true,fraserValleyRelevant:false,impact:'Unclassified',why:'Check source manually. Housing news may affect starts, supply, affordability, and absorption assumptions.',cedarbrookImpact:'Check source manually. Housing news may affect starts, supply, affordability, and absorption assumptions.',predictionHint:'If housing demand remains resilient while supply is constrained, Cedarbrook release timing may improve.',summary:''}];
const $=id=>document.getElementById(id);function get(k){return JSON.parse(localStorage.getItem(k)||'[]')}function set(k,v){localStorage.setItem(k,JSON.stringify(v))}function toast(m){const t=$('toast');t.textContent=m;t.classList.remove('hidden');setTimeout(()=>t.classList.add('hidden'),1800)}
function stars(n){n=Math.max(1,Math.min(5,Number(n||3)));return '★★★★★'.slice(0,n)+'☆☆☆☆☆'.slice(0,5-n)}function starsFromScore(s){s=Number(s||0);return s>=90?5:s>=75?4:s>=60?3:s>=40?2:1}function impactClass(i){return i==='Bullish'?'green':i==='Neutral'?'yellow':i==='Bearish'?'red':'gray'}function relevanceClass(a){return (a.relevanceScore||0)>=80||a.relevanceLevel==='High'?'green':(a.relevanceScore||0)>=60||a.relevanceLevel==='Medium'?'yellow':'gray'}
function normalizeArticle(a){const score=Number(a.relevanceScore||(Number(a.relevance||3)*20));const impact=a.cedarbrookImpact||a.why||'Review this source for possible Chilliwack land development implications.';return{id:a.id||('article-'+Date.now()+Math.random()),title:a.title||'Untitled article',source:a.source||'Unknown source',url:a.url||a.link||'',link:a.link||a.url||'',category:a.category||'General',tier:a.tier||'Source',relevance:Number(a.relevance||starsFromScore(score)),relevanceScore:score,relevanceLevel:a.relevanceLevel||(score>=80?'High':score>=60?'Medium':score>=40?'Watch':'Low'),highRelevance:typeof a.highRelevance==='boolean'?a.highRelevance:score>=80,chilliwackRelevant:!!a.chilliwackRelevant,fraserValleyRelevant:!!a.fraserValleyRelevant,impact:a.impact||'Unclassified',cedarbrookImpact:impact,why:impact,signalType:a.signalType||a.category||'General',predictionHint:a.predictionHint||
`Track whether this signal changes ${a.category||'market'} conditions for Cedarbrook.`,

predictionText:a.predictionText||
a.predictionHint||
'',

executiveTakeaway:a.executiveTakeaway||
'',

summary:a.summary||'',
published:a.published||null
async function loadLiveNews(){$('sourceStatus').innerHTML='<div class="empty">Loading live RSS articles...</div>';try{const response=await fetch('/.netlify/functions/rss-feed',{cache:'no-store'});if(!response.ok)throw new Error('Function returned HTTP '+response.status);const data=await response.json();sourceStatus=data.sources||[];articles=(data.articles||[]).map(normalizeArticle);if(!articles.length){articles=fallbackArticles.map(normalizeArticle);toast('No live articles returned. Showing fallback sources.')}localStorage.setItem('ceiAggregatorArticlesLive',JSON.stringify(articles));localStorage.setItem('ceiAggregatorSourceStatus',JSON.stringify(sourceStatus));$('lastUpdated').textContent=data.generatedAt?'Last updated: '+new Date(data.generatedAt).toLocaleString():'Last updated just now';toast('Live news refreshed');render()}catch(error){sourceStatus=[{source:'RSS Function',ok:false,error:error.message,count:0}];const stored=JSON.parse(localStorage.getItem('ceiAggregatorArticlesLive')||'null');articles=(stored||fallbackArticles).map(normalizeArticle);$('lastUpdated').textContent='Live refresh failed: '+error.message;toast('Live refresh failed. Showing stored/fallback articles.');render()}}
function loadStored(){const stored=JSON.parse(localStorage.getItem('ceiAggregatorArticlesLive')||'null');const storedStatus=JSON.parse(localStorage.getItem('ceiAggregatorSourceStatus')||'null');articles=(stored||[]).map(normalizeArticle);sourceStatus=storedStatus||[];render();loadLiveNews()}
function updateCounters(){const saved=get(STORAGE.savedSignals),talking=get(STORAGE.talking),preds=get(STORAGE.predictions),briefs=get(STORAGE.briefs),reviewed=get(STORAGE.reviewed);const total=reviewed.length+saved.length+talking.length+preds.length+briefs.length;$('pipelineTotal').textContent=total;$('articlesReviewedCount').textContent=reviewed.length;$('savedSignalsCount').textContent=saved.length;$('talkingPointsCount').textContent=talking.length;$('predictionsCount').textContent=preds.length;$('briefItemsCount').textContent=briefs.length;const pct=Math.min(100,Math.round(total/weeklyGoal*100));$('weeklyPill').textContent=pct+'% weekly goal';$('weeklyBar').style.width=pct+'%';$('articleCount').textContent=articles.length;$('highRelevanceCount').textContent=articles.filter(a=>a.highRelevance||a.relevanceScore>=80||a.relevanceLevel==='High').length;$('housingCount').textContent=articles.filter(a=>a.category==='Housing').length;$('ratesCount').textContent=articles.filter(a=>a.category==='Rates').length;generateDigest()}
function reviewed(article){let r=get(STORAGE.reviewed);if(!r.some(x=>x.id===article.id)){r.unshift({id:article.id,title:article.title,date:new Date().toISOString()});set(STORAGE.reviewed,r)}}function classify(article,impact){article.impact=impact;reviewed(article);localStorage.setItem('ceiAggregatorArticlesLive',JSON.stringify(articles));toast('Marked '+impact);render()}function saveSignal(article){let arr=get(STORAGE.savedSignals);if(!arr.some(x=>x.id===article.id))arr.unshift({...article,savedAt:new Date().toISOString()});set(STORAGE.savedSignals,arr);reviewed(article);toast('Signal saved');updateCounters()}function talkingPoint(article){let arr=get(STORAGE.talking);arr.unshift({id:Date.now(),text:`${article.category}: ${article.title}. ${article.cedarbrookImpact||article.why}`,source:article.source,createdAt:new Date().toISOString()});set(STORAGE.talking,arr);reviewed(article);toast('Talking point added');updateCounters()}function prediction(article){let arr=get(STORAGE.predictions);arr.unshift({id:Date.now(),title:article.predictionHint||`${article.title} will influence ${article.category.toLowerCase()} conditions`,articleTitle:article.title,source:article.source,category:article.category,confidence:article.relevanceLevel==='High'?'High':'Medium',relevanceScore:article.relevanceScore,status:'Pending',createdAt:new Date().toISOString()});set(STORAGE.predictions,arr);reviewed(article);toast('Prediction created');updateCounters()}function brief(article){let arr=get(STORAGE.briefs);arr.unshift({id:Date.now(),title:article.title,source:article.source,category:article.category,impact:article.impact,relevanceScore:article.relevanceScore,summary:article.cedarbrookImpact||article.why,createdAt:new Date().toISOString()});set(STORAGE.briefs,arr);reviewed(article);toast('Added to brief');updateCounters()}
function sendDashboard(article){let dash={};try{dash=JSON.parse(localStorage.getItem(STORAGE.dashboard)||'{}')}catch{}if(!dash.signals)dash.signals=[];if(!dash.signals.some(s=>s.text===article.title)){dash.signals.unshift({category:article.category,text:article.title,impact:article.impact==='Unclassified'?'Neutral':article.impact,confidence:article.relevanceLevel==='High'?'High':'Medium'});dash.signals=dash.signals.slice(0,5)}localStorage.setItem(STORAGE.dashboard,JSON.stringify(dash));reviewed(article);toast('Sent to dashboard');updateCounters()}function openSource(article){const u=article.url||article.link;if(u)window.open(u,'_blank','noopener');else toast('No source URL')}
function renderSourceStatus(){const wrap=$('sourceStatus');wrap.innerHTML='';if(!sourceStatus.length){wrap.innerHTML='<div class="empty">No source status yet.</div>';return}sourceStatus.forEach(s=>{const div=document.createElement('div');div.className='source-row '+(s.ok?'ok':'bad');div.innerHTML=`<strong>${s.source}</strong><span>${s.ok?'OK':'Issue'} · ${s.count||0} articles${s.error?' · '+s.error:''}</span>`;wrap.appendChild(div)})}
function articleMatchesFilter(a,f){if(f==='All')return true;if(f==='High Relevance')return a.highRelevance||a.relevanceScore>=80||a.relevanceLevel==='High';if(f==='Chilliwack+')return a.chilliwackRelevant||a.fraserValleyRelevant;if(f==='Fraser Valley')return a.fraserValleyRelevant||a.title.includes(f)||a.source.includes(f);if(f==='Chilliwack')return a.chilliwackRelevant||a.title.includes(f)||a.source.includes(f);return a.category===f||a.title.includes(f)||a.source.includes(f)}
function render(){renderSourceStatus();const feed=$('feed');feed.innerHTML='';$('activeFilter').textContent=activeFilter;const filtered=articles.filter(a=>articleMatchesFilter(a,activeFilter));if(!filtered.length){feed.innerHTML='<div class="empty">No articles available for this filter. Tap Refresh Live News or choose All.</div>';updateCounters();return}filtered.forEach(article=>{const node=$('articleTemplate').content.cloneNode(true);node.querySelector('.source-line').textContent=article.source;node.querySelector('.article-title').textContent=article.title;node.querySelector('.stars').textContent=stars(article.relevance||starsFromScore(article.relevanceScore));node.querySelector('.category-tag').textContent=article.category;node.querySelector('.tier-tag').textContent=article.tier;const rel=node.querySelector('.relevance-score-tag');rel.textContent=`${article.relevanceLevel||'Watch'} · ${Math.round(article.relevanceScore||0)}/100`;rel.className='tag relevance-score-tag '+relevanceClass(article);const local=node.querySelector('.local-tag');if(article.chilliwackRelevant||article.fraserValleyRelevant){local.textContent=article.chilliwackRelevant?'Chilliwack+':'Fraser Valley';local.className='tag local-tag blue'}const impact=node.querySelector('.impact-tag');impact.textContent=article.impact;impact.className='tag impact-tag '+impactClass(article.impact);node.querySelector('.date-tag').textContent=article.published?new Date(article.published).toLocaleDateString():'No date';node.querySelector('.why-text').textContent=article.cedarbrookImpact||article.why;node.querySelector('.summary-text').textContent=article.summary||'No source summary provided.';node.querySelectorAll('[data-impact]').forEach(b=>b.onclick=()=>classify(article,b.dataset.impact));node.querySelector('.save-signal').onclick=()=>saveSignal(article);node.querySelector('.talking-point').onclick=()=>talkingPoint(article);node.querySelector('.prediction').onclick=()=>prediction(article);node.querySelector('.brief').onclick=()=>brief(article);node.querySelector('.dashboard').onclick=()=>sendDashboard(article);node.querySelector('.open-source').onclick=()=>openSource(article);feed.appendChild(node)});updateCounters()}
function generateDigest(){const saved=get(STORAGE.savedSignals),talking=get(STORAGE.talking),preds=get(STORAGE.predictions),briefs=get(STORAGE.briefs);const lines=['CEI Intelligence Pipeline Digest','',`Signals saved: ${saved.length}`,`Talking points: ${talking.length}`,`Predictions: ${preds.length}`,`Brief items: ${briefs.length}`,'','Top talking points:'];talking.slice(0,3).forEach((t,i)=>lines.push(`${i+1}. ${t.text}`));$('digestOutput').value=lines.join('\n')}
function addManual(){const title=$('manualTitle').value.trim();if(!title){toast('Add a title first');return}const cat=$('manualCategory').value;const why=$('manualWhy').value.trim()||'Manual interpretation not added yet.';const article=normalizeArticle({id:'manual-'+Date.now(),title,source:$('manualSource').value.trim()||'Manual source',url:$('manualUrl').value.trim(),link:$('manualUrl').value.trim(),category:cat,tier:'Manual',relevance:4,relevanceScore:75,relevanceLevel:'Medium',highRelevance:false,chilliwackRelevant:cat==='Chilliwack'||cat==='Fraser Valley',fraserValleyRelevant:cat==='Fraser Valley',impact:'Unclassified',cedarbrookImpact:why,why,summary:'',published:new Date().toISOString()});articles.unshift(article);localStorage.setItem('ceiAggregatorArticlesLive',JSON.stringify(articles));['manualTitle','manualSource','manualUrl','manualWhy'].forEach(id=>$(id).value='');toast('Article added');render()}
document.querySelectorAll('#filters button').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('#filters button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');activeFilter=btn.dataset.filter;render()});$('refreshBtn').onclick=loadLiveNews;$('clearManualBtn').onclick=()=>['manualTitle','manualSource','manualUrl','manualWhy'].forEach(id=>$(id).value='');$('addManualBtn').onclick=addManual;$('copyDigestBtn').onclick=()=>navigator.clipboard.writeText($('digestOutput').value||'');if('serviceWorker'in navigator){navigator.serviceWorker.register('./news-aggregator-sw.js').catch(()=>{})}loadStored();
